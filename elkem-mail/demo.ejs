<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Neon Device Dashboard</title>
    <style>
        :root {
            --primary: #2A2A36;
            --secondary: #5A5A6F;
            --background: #F8F9FF;
            --surface: #FFFFFF;
            --accent: #6A5BFF;
            --accent2: #00C1D4;
            --border: #E3E4ED;
        }

        body {
            font-family: 'Inter', system-ui, sans-serif;
            margin: 0;
            min-height: 100vh;
            background: linear-gradient(135deg, #F8F9FF 0%, #EDEEF5 100%);
            color: var(--primary);
            display: grid;
            place-items: center;
        }

        .container {
            width: 1200px;
            background: var(--surface);
            border-radius: 24px;
            padding: 32px;
            box-shadow: 0 12px 40px rgba(42,42,54,0.08);
        }

        .header {
            margin-bottom: 32px;
            padding-bottom: 24px;
            border-bottom: 2px solid var(--border);
        }

        h1 {
            font-size: 28px;
            margin: 0;
            color: var(--accent);
            font-weight: 700;
            position: relative;
            padding-left: 12px;
        }

        h1::before {
            content: "";
            position: absolute;
            left: 0;
            top: 4px;
            bottom: 4px;
            width: 4px;
            background: var(--accent);
            border-radius: 2px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 24px;
        }

        th, td {
            padding: 20px;
            text-align: left;
        }

        th {
            font-weight: 600;
            color: var(--secondary);
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        tr {
            border-bottom: 1px solid var(--border);
            transition: all 0.2s ease;
        }

        tr:hover {
            background: rgba(106,91,255,0.03);
            transform: translateX(8px);
        }

        .status-indicator {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 8px 16px;
            border-radius: 999px;
            font-size: 14px;
            font-weight: 500;
            background: rgba(106,91,255,0.08);
            color: var(--accent);
        }

        .status-indicator::before {
            content: "";
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background: var(--accent);
        }

        .offline {
            background: rgba(90,90,111,0.08);
            color: var(--secondary);
        }

        .offline::before {
            background: var(--secondary);
        }

        .device-id {
            font-weight: 600;
            color: var(--primary);
            font-size: 16px;
        }

        .timestamp {
            color: var(--secondary);
            font-size: 14px;
            margin-top: 4px;
        }

        .data-value {
            font-size: 16px;
            font-weight: 500;
            color: var(--accent2);
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Device Nexus</h1>
        </div>

        <table>
            <thead>
                <tr>
                    <th>Status</th>
                    <th>Device</th>
                    <th>Activity</th>
                    <th>Last Sync</th>
                </tr>
            </thead>
            <tbody>
                <% data.forEach(entry => { %>
                    <tr>
                        <td>
                            <div class="status-indicator <%= entry.status === 'Online' ? '' : 'offline' %>">
                                <%= entry.status %>
                            </div>
                        </td>
                        <td class="device-id"><%= entry.name %></td>
                        <td>
                            <div class="data-value"><%= entry.relativeTime %></div>
                            <div class="timestamp"><%= entry.type %></div>
                        </td>
                        <td>
                            <div class="timestamp"><%= entry.timestamp_ist %></div>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
</body>
</html> -->


<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Daily Device Status Report</title>
    <style>
        :root {
            --primary: #2A2A36;
            --secondary: #5A5A6F;
            --background: #F8F9FF;
            --surface: #FFFFFF;
            --accent: #6A5BFF;
            --accent2: #00C1D4;
            --border: #E3E4ED;
        }

        body {
            font-family: 'Inter', system-ui, sans-serif;
            margin: 0;
            min-height: 100vh;
            background: var(--background);
            color: var(--primary);
            display: grid;
            place-items: center;
        }

        .container {
            width: 1200px;
            background: var(--surface);
            border-radius: 24px;
            padding: 32px;
            box-shadow: 0 12px 40px rgba(42,42,54,0.08);
        }

        .header {
            margin-bottom: 32px;
            padding-bottom: 24px;
            border-bottom: 2px solid var(--border);
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        h1 {
            font-size: 28px;
            margin: 0;
            color: var(--primary);
            font-weight: 700;
        }

        .report-summary {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
            margin-bottom: 32px;
        }

        .summary-card {
            padding: 24px;
            border-radius: 12px;
            background: rgba(106,91,255,0.05);
        }

        .summary-card h3 {
            margin: 0 0 8px 0;
            font-size: 16px;
            color: var(--secondary);
        }

        .summary-card p {
            margin: 0;
            font-size: 28px;
            font-weight: 700;
            color: var(--accent);
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 24px;
        }

        th, td {
            padding: 16px 20px;
            text-align: left;
        }

        th {
            font-weight: 600;
            color: var(--secondary);
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            background: rgba(106,91,255,0.05);
        }

        tr {
            border-bottom: 1px solid var(--border);
        }

        .status-indicator {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 8px 16px;
            border-radius: 999px;
            font-size: 14px;
            font-weight: 500;
        }

        .online-status {
            background: rgba(106,91,255,0.08);
            color: var(--accent);
        }

        .online-status::before {
            content: "";
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background: var(--accent);
        }

        .offline-status {
            background: rgba(255,91,91,0.08);
            color: #FF5B5B;
        }

        .offline-status::before {
            content: "";
            width: 8px;
            height: 8px;
            border-radius: 50%;
            background: #FF5B5B;
        }

        .device-name {
            font-weight: 600;
            color: var(--primary);
        }

        .last-activity {
            color: var(--secondary);
            font-size: 14px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="timestamp"><%= new Date().toLocaleDateString('en-IN', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }) %></div>
        </div>

        <% 
            let onlineCount = 0;
            let offlineCount = 0;
            data.forEach(device => {
                if(device.status === 'Online') onlineCount++;
                else offlineCount++;
            });
        %>

        <div class="report-summary">
            <div class="summary-card">
                <h3>Total Devices</h3>
                <p><%= data.length %></p>
            </div>
            <div class="summary-card">
                <h3>Online Devices</h3>
                <p><%= onlineCount %></p>
            </div>
            <div class="summary-card">
                <h3>Offline Devices</h3>
                <p><%= offlineCount %></p>
            </div>
        </div>

        <table>
            <thead>
                <tr>
                    <th>Status</th>
                    <th>Device Name</th>
                    <th>Device Type</th>
                    <th>Last Activity</th>
                </tr>
            </thead>
            <tbody>
                <% data.forEach(device => { %>
                    <tr>
                        <td>
                            <div class="status-indicator <%= device.status === 'Online' ? 'online-status' : 'offline-status' %>">
                                <%= device.status %>
                            </div>
                        </td>
                        <td class="device-name"><%= device.name %></td>
                        <td><%= device.type %></td>
                        <td class="last-activity"><%= device.relativeTime %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
</body>
</html> -->
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Daily Device Status Report</title>
</head>
<body style="margin: 0; padding: 20px; background-color: #f5f5f5; font-family: 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;">
    
    <!-- Main Container -->
    <div style="max-width: 800px; margin: 0 auto; background: white; border-radius: 8px; box-shadow: 0 2px 8px rgba(0,0,0,0.1);">
        
        <!-- Header -->
        <div style="padding: 24px 32px; background: #6A5BFF; border-radius: 8px 8px 0 0;">
            <table width="100%">
                <tr>
                    <td>
                        <img src="https://senselive.io/logo/logos/Square_White.png" alt="Logo" style="height: 75px;">
                    </td>
                    <td style="text-align: right; color: white;">
                        <div style="font-size: 14px; margin-bottom: 4px;"><%= new Date().toLocaleDateString('en-IN') %></div>
                        <div style="font-size: 12px;">Daily Status Report</div>
                    </td>
                </tr>
            </table>
        </div>

        <!-- Summary Cards -->
        <div style="padding: 24px 32px 0 32px;">
            <table width="100%" style="border-collapse: collapse;">
                <tr>
                    <td style="padding: 16px; background: #f8f9ff; border-radius: 8px; text-align: center; margin: 4px;">
                        <div style="font-size: 12px; color: #5A5A6F; margin-bottom: 8px;">Total Devices</div>
                        <div style="font-size: 24px; font-weight: 600; color: #2A2A36;"><%= data.length %></div>
                    </td>
                    <td style="padding: 16px; background: #e3f8fa; border-radius: 8px; text-align: center; margin: 4px;">
                        <div style="font-size: 12px; color: #5A5A6F; margin-bottom: 8px;">Online</div>
                        <div style="font-size: 24px; font-weight: 600; color: #00C1D4;"><%= onlineCount %></div>
                    </td>
                    <td style="padding: 16px; background: #f5f5f5; border-radius: 8px; text-align: center; margin: 4px;">
                        <div style="font-size: 12px; color: #5A5A6F; margin-bottom: 8px;">Offline</div>
                        <div style="font-size: 24px; font-weight: 600; color: #5A5A6F;"><%= offlineCount %></div>
                    </td>
                </tr>
            </table>
        </div>

        <!-- Device Table -->
        <div style="padding: 24px 32px;">
            <table width="100%" style="border-collapse: collapse;">
                <thead>
                    <tr style="background-color: #f8f9ff;">
                        <th style="padding: 12px 16px; text-align: left; font-size: 12px; color: #5A5A6F;">Status</th>
                        <th style="padding: 12px 16px; text-align: left; font-size: 12px; color: #5A5A6F;">Device Name</th>
                        <th style="padding: 12px 16px; text-align: left; font-size: 12px; color: #5A5A6F;">Type</th>
                        <th style="padding: 12px 16px; text-align: left; font-size: 12px; color: #5A5A6F;">Last Activity</th>
                    </tr>
                </thead>
                <tbody>
                    <% data.forEach(device => { %>
                    <tr style="border-bottom: 1px solid #eee;">
                        <td style="padding: 16px;">
                            <div style="display: inline-flex; align-items: center; padding: 4px 12px; border-radius: 16px; font-size: 12px; background-color: <%= device.status === 'Online' ? '#e8e5ff' : '#eeeeee' %>; color: <%= device.status === 'Online' ? '#6A5BFF' : '#5A5A6F' %>;">
                                ● <span style="margin-left: 8px;"><%= device.status %></span>
                            </div>
                        </td>
                        <td style="padding: 16px; font-weight: 500; color: #2A2A36;"><%= device.name %></td>
                        <td style="padding: 16px; color: #5A5A6F;"><%= device.type %></td>
                        <td style="padding: 16px; color: #5A5A6F; font-size: 14px;"><%= device.relativeTime %></td>
                    </tr>
                    <% }) %>
                </tbody>
            </table>
        </div>

        <!-- Footer -->
        <div style="padding: 24px 32px; background: #f8f9ff; border-radius: 0 0 8px 8px; font-size: 12px; color: #5A5A6F;">
            <div style="margin-bottom: 8px;">Need assistance? Contact our support team</div>
            <div style="font-size: 11px; color: #888;">This is an automated report - please do not reply</div>
        </div>
    </div>

</body>
</html>